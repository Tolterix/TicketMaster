import React from 'react';
import { shallow } from 'enzyme';

import  from '';

describe('', () => {
    var wrapper;
    beforeEach(() => {
        wrapper = shallow();
    });

    describe('#handleSubmit()', () => {
        it('', () => {
        });
    });
});
